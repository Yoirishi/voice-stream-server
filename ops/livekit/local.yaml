port: 7880

rtc:
  port_range_start: 50000
  port_range_end: 50020
  tcp_port: 7881
  use_external_ip: false
  # Local Docker Compose only. For real internet deployment this must be replaced
  # with a public IP/domain + proper TURN/TLS configuration.
  node_ip: 127.0.0.1

keys:
  devkey: secret

room:
  empty_timeout: 300
  departure_timeout: 20
